# stem-hub 模块集成与使用说明

> 适用固件：`release-v3.3`
> 主要读者：上位机与系统集成工程师
> 协议参考：[上位机 AT 命令文档](./上位机AT命令文档.md)
> 现场操作：[stem-hub 模块操作说明](./stem-hub模块操作说明.md)

本文是 `release-v3.3` 的产品集成主文档：说明模块能做什么、如何安全接入，以及主机如何依据实际状态完成业务闭环。精确 AT 命令语法、参数范围、响应格式和错误码以[上位机 AT 命令文档](./上位机AT命令文档.md)为唯一依据；现场人员的逐步操作以[stem-hub 模块操作说明](./stem-hub模块操作说明.md)为准。

## 1. 产品概述

stem-hub 为主机提供一条统一的设备控制与状态回传通道：

- **UART1 控制面**：主机通过 AT 请求读取状态、设置工作方式并接收结果。
- **UART2/UART3 下游通信**：主机可按需打开下游通道、发送负载数据，并在 UART1 上接收带来源标识的回传事件。
- **电源路径**：支持关闭、充电与驱动三类请求模式，充电工作具有 ON/OFF 实际相位。
- **电机与辅助输出**：可控制电机模式、相关旁路与允许的辅助输出；这些能力受状态联锁和保护约束。
- **传感与诊断**：主机可读取传感快照、故障输入和运行状态，用于显示、告警与恢复决策。
- **保护**：温度保护与堵转保护可强制系统进入安全状态，主机须以状态回读确认最终结果。
- **故障输入**：`nFAULT`/`nFLT` 当前仅提供故障输入状态查询，不触发自动安全动作，也不承诺自动恢复。

## 2. 适用范围与责任边界

### 本文覆盖

- UART1 上的集成流程、状态同步、请求调度和异常恢复；
- 充电、驱动、电机、下游通信与安全停止的业务闭环；
- 可由主机查询和确认的状态、保护与验收要求。

### 本文不定义

本文**不定义**供电范围、连接器、物理电平、机械安装或环境等级。上述信息以后续独立硬件规格书为准，集成前不得从本文或 AT 协议推断额定值、接口制式或安装条件。

### 责任分工

| 角色 | 应负责的结果 |
| --- | --- |
| 模块固件 | 执行已接受的控制请求、维护可查询状态、实施既有联锁与保护，并按协议返回同步响应或异步事件。 |
| 上位机/系统集成 | 完成版本握手、串行调度、状态回读、事件分流、超时恢复与 UI/业务门控。 |
| 硬件与现场集成 | 按独立硬件规格完成供电、接线、负载、安装和环境条件确认。 |

## 3. 五分钟快速接入

以下流程只建立可信的连接与初始状态，不启动充电、驱动或电机。

1. 以 **9600、8N1、无流控** 打开 UART1。
   - 成功现象：串口打开成功，接收器已能按完整 `CRLF` 行累计数据。
2. 发送 `AT+VERSION?\r\n`。
   - 成功现象：收到精确响应 `+VERSION:release-v3.3\r\nOK\r\n`，才判定本协议版本匹配。
3. 在每项终止响应完成后，依次查询 `OUTPUT`、`MOTOR`、`CHARGE_TIME`、`STALL_CURRENT`、`FAULT` 和 `SENSE`。
   - 成功现象：每个查询均收到其数据行及同步终止响应；主机可建立电源、输出、电机、配置、故障与传感的初始视图。
4. 若 `SENSE` 返回 `SENSE_NOT_READY`，只延迟后重试 **SENSE** 查询。
   - 成功现象：版本握手和其他状态仍保持有效；取得首个传感快照后再补全传感视图，而不重新判定连接失败。

如版本不是 `release-v3.3`，进入“不兼容”状态：保留只读诊断的可能性，但拒绝发送充电、驱动、电机或辅助输出等危险控制，直到安装了该版本对应的协议适配器。

## 4. 产品功能与状态模型

### 4.1 电源请求模式与实际充电相位

`POWER` 和 `CHARGE_PHASE` 描述不同层次，主机不得混用。

| 字段 | 值 | 含义 | 主机应如何解读 |
| --- | --- | --- | --- |
| `POWER` | `OFF` | 请求关闭电源路径 | 充电与驱动均不应处于工作请求。 |
| `POWER` | `CHARGE` | 请求充电模式 | 充电周期正在管理实际相位；不能据此断言当前一定导通。 |
| `POWER` | `DRIVE` | 请求驱动模式 | 驱动路径为目标状态，可进一步请求允许的辅助输出。 |
| `CHARGE_PHASE` | `IDLE` | 当前不处于充电周期 | 预期与 `POWER=OFF` 或 `POWER=DRIVE` 对应。 |
| `CHARGE_PHASE` | `ON` | 充电周期当前 ON 相位 | 这是当前充电路径状态。 |
| `CHARGE_PHASE` | `OFF` | 充电周期当前 OFF 相位 | `POWER` 仍可能为 `CHARGE`；等待周期下一相位或重新查询。 |

充电使用固定的 60 秒周期，`CHARGE_TIME` 指定每周期 ON 时长。设为 60 秒时可持续处于 ON 相位；其余时间为 OFF 相位。`CHARGE_TIME` 仅保存在 RAM，复位后采用默认值；`STALL_CURRENT` 写入 Flash，成功设置后跨复位保留。

### 4.2 输出联锁与复位安全状态

- 只有在 `DRIVE` 下，NMOS1、NMOS2 与 `LIGHTS` 才允许开启；主机必须先回读确认 `POWER=DRIVE`。
- 离开 `DRIVE` 时，NMOS1、NMOS2 和 `LIGHTS` 会自动关闭；界面应按 `OUTPUT` 回读禁用这些项目。
- `CHARGE_BYPASS` 仅可在 `POWER=CHARGE` 且硬件/预充条件满足时开启；它跨 `CHARGE_PHASE=OFF` 保持。主机以 `OUTPUT.CHARGE_BYPASS` 回读确认，退出 CHARGE、温度保护或复位时该旁路自动关闭。
- 复位后系统回到安全状态：电源请求为 `OFF`、充电相位为 `IDLE`、电机休眠，危险辅助输出和旁路均关闭，下游桥接关闭。
- 电源路径请求、辅助输出和电机动作可能先被接受后再完成。`OK` 不是所有控制类命令“已观察到生效”的同义词，最终以 `OUTPUT` 或 `MOTOR` 回读为准。

### 4.3 电机与下游通信状态

电机状态可为 `SLEEP`、`WAKE`、`FWD`（正向）、`REV`（反向）、`BRAKE` 或 `STOP`。正反向之间的换向由模块先进入安全过渡后完成；主机只下发目标模式并等待状态确认，不推断中间电气状态。电机在运动状态下可使用相关旁路，离开运动、换向或保护停机时旁路会回到关闭状态。

UART2 和 UART3 是下游通道而非第二个主机控制口。收到的下游字节会作为 `+UART2RX:` 或 `+UART3RX:` 异步事件从 UART1 返回；事件的分段不等同于下游业务报文边界，主机需要按下游协议自行重组。

## 5. 标准业务流程

本节用业务目标定义闭环。每条命令的精确写法、字段与错误响应均请查阅[上位机 AT 命令文档](./上位机AT命令文档.md)。除明确标注的异步事件外，每个操作均应等待当前请求的同步终止响应后再发送下一条。

### 5.1 建立连接并同步状态

**前置条件**：UART1 已按 9600 8N1、无流控连接；主机接收器能按 `CRLF` 分行。

**操作顺序**：

1. 发送版本查询，并精确匹配 `release-v3.3`。
2. 依次查询 `OUTPUT`、`MOTOR`、`CHARGE_TIME`、`STALL_CURRENT`、`FAULT` 和 `SENSE`。
3. 仅对 `SENSE_NOT_READY` 延迟重试 `SENSE`；对其他失败按错误码处理。
4. 将所有已读字段写入本地状态模型，再开放与当前实态相符的控制入口；危险控制入口仍须受 6.4 的温度门控。

**成功判据**：版本精确匹配；每项关键状态字段完整且合法；主机能够显示当前电源模式、充电相位、电机状态、保护/故障和配置值。

**失败处理**：版本未知时拒绝危险控制；通信、字段或终止响应异常时关闭控制入口，重新执行握手与全量同步，不能沿用断线前缓存。

### 5.2 进入和退出充电模式

**前置条件**：已完成连接同步并满足 6.4 的危险动作温度门控；硬件与负载条件已由独立规格确认。

**操作顺序**：

1. 如业务要求，先设置并回读 `CHARGE_TIME`。
2. 请求进入充电模式。
3. 收到 `OK` 后查询 `OUTPUT`，必要时在后续周期再次查询充电相位。
4. 如需充电旁路，只在已确认 `POWER=CHARGE` 且硬件/预充条件满足时请求开启，并通过 `OUTPUT.CHARGE_BYPASS` 回读确认；该旁路可跨 `CHARGE_PHASE=OFF` 保持。
5. 退出前关闭充电旁路，再请求完整 `OFF`，并查询 `OUTPUT`。

**成功判据**：进入后 `POWER=CHARGE`，`CHARGE_PHASE` 为 `ON` 或 `OFF`；若请求充电旁路，`OUTPUT.CHARGE_BYPASS` 为开启且在 OFF 相位仍保持。退出后 `POWER=OFF`、`CHARGE_PHASE=IDLE` 且 `OUTPUT.CHARGE_BYPASS` 为关闭。进入充电时，驱动子输出不应保持开启。

**失败处理**：`OK` 只表示循环请求被接受，**不表示充电路径此刻一定处于 ON 相位**。若充电旁路前置条件不满足或回读未开启，保持旁路关闭。若其他回读未达到目标、出现保护或通信超时，停止后续开启操作，查询 `FAULT`、`OUTPUT` 与 `SENSE`，再按实态恢复。

### 5.3 进入和退出驱动模式

**前置条件**：已完成连接同步并满足 6.4 的危险动作温度门控；辅助负载满足硬件规格。

**操作顺序**：

1. 请求进入驱动模式，收到 `OK` 后回读 `OUTPUT`。
2. 仅在确认 `POWER=DRIVE` 后，按业务需要逐项请求 NMOS1、NMOS2，或请求 LED 联动开关；每次都通过 `OUTPUT` 回读确认，其中 LED 联动以 `OUTPUT.LIGHTS` 确认。
3. 退出时请求完整 `OFF`，再回读 `OUTPUT`。

**成功判据**：进入后 `POWER=DRIVE`；被请求且允许的子项与回读一致。退出后 `POWER=OFF`、`CHARGE_PHASE=IDLE`，NMOS1、NMOS2 和 `LIGHTS` 均为关闭。

**失败处理**：在 `OFF` 或 `CHARGE` 下请求开启驱动子项属于联锁失败，应保持或恢复关闭状态而非重试开启。任何超时先按“可能已执行”处理，重新握手和回读后再决策。

### 5.4 启动、调节、换向和停止电机

**前置条件**：已完成连接同步并满足 6.4 的危险动作温度门控；操作者已确认机构、负载与周边安全；需要调整堵转阈值时，电机不处于 `FWD`/`REV` 运行。

**操作顺序**：

1. 启动前查询 `MOTOR` 与 `FAULT`；必要时查询并设置 `STALL_CURRENT`，随后回读配置。
2. 请求 `FWD`（正向）或 `REV`（反向）运行，收到 `OK` 后查询 `MOTOR` 确认目标模式。
3. 需要旁路时，只在已确认 `FWD` 或 `REV` 稳定后请求开启，并通过 `OUTPUT` 回读确认。
4. 换向时直接请求新的 `FWD` 或 `REV` 目标，然后等待模块完成安全状态、方向切换和回读确认；不要自行控制或推断中间状态。
5. 普通停止时先关闭旁路，再请求 `STOP` 或 `BRAKE`，并通过 `MOTOR` 和 `OUTPUT` 确认对应停止模式及电机旁路关闭。需要最低静态功耗或执行安全关机时，再请求 `SLEEP` 并回读确认。

**成功判据**：运行或换向后的 `MOTOR` 模式与目标一致。普通停止后为 `STOP` 或 `BRAKE`，且电机旁路关闭；需要低功耗或安全关机时为 `SLEEP`，电机旁路同样关闭。所有情况均不得有未处理的堵转或故障状态。

**失败处理**：如果发生堵转，先停止自动重启，查询 `MOTOR` 与 `FAULT`，排除机械或负载原因后才由操作者重新启动。状态未确认前，不允许把界面显示更新为已完成。

### 5.5 打开 UART2/UART3 并进行下游通信

**前置条件**：已完成连接同步；下游设备协议已明确；主机具有独立的异步事件处理器。

**操作顺序**：

1. 按需打开 UART2、UART3 或两者的桥接，并等待同步终止响应。
2. 用 AT 协议规定的下游发送方式发送数据。
3. 持续分流 `+UART2RX:` 与 `+UART3RX:` 事件，并按下游协议拼接、解析和应答。
4. 结束业务时关闭桥接；断线或复位后将桥接视为关闭并重新配置。

**成功判据**：桥接开关请求成功结束；下游数据按预期到达；回传事件来源正确且业务帧由主机重组完整。

**失败处理**：下游无响应不等同于 UART1 控制失败。记录原始事件与时间戳，检查下游设备和协议；不可把异步事件误作当前 AT 请求的数据或终止响应。

### 5.6 安全停止并断开连接

**前置条件**：业务结束、准备断线、发生异常，或需要交还设备。

**操作顺序**：

1. 关闭电机相关旁路，请求电机停止并进一步请求休眠。
2. 关闭所需的辅助输出，随后请求完整 `OFF`。
3. 关闭 UART2/UART3 桥接。
4. 查询 `MOTOR`、`OUTPUT` 与 `FAULT`，确认安全状态后断开 UART1。

**成功判据**：`MOTOR` 为 `SLEEP`；`POWER=OFF`、`CHARGE_PHASE=IDLE`；NMOS1、NMOS2、`LIGHTS` 和两类旁路均关闭。

**失败处理**：任一回读缺失、超时或不一致时不要假设停机已经完成。保持主机处于受控异常状态，尝试重新握手、回读并重复安全关闭意图；仍无法通信时按系统级安全策略处理现场风险。

## 6. 上位机集成规则

### 6.1 请求与响应调度

- 对一条 UART1 连接实行**单通道串行请求**：同一时刻最多一个在途 AT 请求。
- 必须等待该请求的同步终止响应后，才能发出下一条。查询的状态数据行不是结束标志；最终 `OK` 或 `ERROR...` 才是。
- 按完整 `CRLF` 行解析接收流。一次串口读取可能包含半行、多行或异步事件，不能以读调用边界拆分业务含义。
- 对控制命令，`OK` 表示的粒度取决于协议类型。任何涉及电源、辅助输出或电机的最终显示与下一步动作，都必须由 `OUTPUT` 或 `MOTOR` 状态回读确认。

### 6.2 异步事件分流

`+UART2RX:` 和 `+UART3RX:` 是异步下游事件，可能在当前请求的数据行之前、数据行与 `OK` 之间或请求结束后出现。接收器应至少分成三条逻辑路径：

| 接收行类别 | 处理方式 |
| --- | --- |
| 当前请求的数据行、`OK`、`ERROR...` | 交给当前请求状态机；仅终止响应结束该请求。 |
| `+UART2RX:`、`+UART3RX:` | 交给下游事件处理器，不结束当前请求。 |
| 未知行或故障现场信息 | 保存原文、时间戳和连接上下文；按错误策略进入诊断或重连。 |

### 6.3 超时、版本与重连

- **超时具有歧义**：它可能是命令未到达、已执行但回包丢失、接收解析失配或设备已复位；超时不能证明命令未执行，也不能直接安全重发危险命令。
- 超时后停止发送新的危险控制，尝试版本查询；无响应则关闭并重新打开串口。
- 每次断线、复位、重新打开串口或收到致命故障信息后，都重新握手并全量同步 `OUTPUT`、`MOTOR`、两项配置、`FAULT` 与 `SENSE`。
- 只接受精确匹配的已知版本。未知版本拒绝危险控制，不以“看起来能响应”作为兼容依据。

### 6.4 危险动作温度门控

充电、驱动、电机及其危险子输出的开启，必须通过以下主机侧保守门控。`SENSE` 只是必要而非充分条件：当前协议没有独立的温度锁存查询字段，单个 `SENSE` 可能是旧快照，不能证明锁存已经解除。

1. 连续查询 `SENSE`，至少比较两次查询的 `TICK`/`COUNT`；在预期约 1 Hz 的采样窗口内确认快照已经推进。
2. 在已推进的快照中，确认五路受保护温度字段全部有效且均不高于 55.0°C。
3. 任一快照未推进、字段无效、温度高于 55.0°C 或保护状态未知时，一律保持危险输出关闭。
4. 即使预检查通过，危险开启命令执行时的固件保护检查仍是最终裁决。若返回 `ERROR:OVER_TEMPERATURE`，继续保持关闭，重新同步状态；不得自动重试开启。

## 7. 安全保护与异常恢复

### 7.1 温度保护

模块对五路受保护器件温度实施锁存保护：MCU、LM51770、MP4317、DRV8874 与充电 MOS。任一路温度无效，或任一路温度**严格高于 60.0°C**，都会触发保护并收敛至安全状态。

- 触发时：电源路径转为完整关闭，电机进入休眠；开启类请求受保护状态限制。
- 解除条件：五路受保护温度必须全部有效且全部**不高于 55.0°C**。
- 解除后的动作：保护锁存自动解除，但充电、驱动、电机和辅助输出**不会自动恢复**；主机必须重新查询状态并通过 6.4 的门控，再由操作者或业务逻辑明确发起新请求。

电池温度等遥测字段可用于业务判断，但不替代上述五路受保护温度的保护判据。

### 7.2 堵转、故障输入与恢复

- 堵转保护触发后，电机进入安全状态并记录过流/堵转状态。恢复前先查询 `MOTOR` 和 `FAULT`，排除机构卡滞、负载异常等原因；禁止无条件自动再启动。
- `nFAULT` 与 `nFLT` 为可查询的故障输入。本版本只提供其状态查询，**不触发自动安全动作，也不承诺自动恢复**故障源或外部硬件。
- 通信超时不能证明控制请求没有执行。恢复时先重连、版本握手和全量状态查询，以设备回读而非主机缓存决定下一步。

## 8. 联调、验收与故障排查

### 8.1 验收清单

- [ ] **版本握手**：`AT+VERSION?` 返回精确的 `release-v3.3` 及 `OK`。
- [ ] **初态同步**：复位后完成 `OUTPUT`、`MOTOR`、配置、`FAULT` 与 `SENSE` 的查询；初态为安全状态。
- [ ] **电源状态**：分别验证 `OFF`、`CHARGE` 与 `DRIVE` 的状态回读；在充电中观察 `CHARGE_PHASE=ON/OFF` 的实际相位语义。
- [ ] **DRIVE 子项联锁**：仅在已确认 `DRIVE` 后能开启 NMOS1、NMOS2 与 `LIGHTS`；离开 DRIVE 后三者自动关闭。
- [ ] **电机状态**：验证启动、停止、休眠、换向和状态回读；换向过程中不以 `OK` 取代最终状态确认。
- [ ] **异步事件**：下游回传的 `+UART2RX:`/`+UART3RX:` 被正确分流，且不提前结束在途请求。
- [ ] **过温停机**：优先通过传感器/ADC 故障注入模拟任一受保护温度无效或超过 60.0°C，验证电源关闭和电机休眠；不物理加热带载器件。温度恢复到解除条件后确认不会自动恢复输出。
- [ ] **堵转停机**：仅使用硬件负责人批准的限流电源、可控负载或较低测试阈值，验证堵转后电机安全状态、状态查询和人工恢复流程；禁止满功率硬堵转和无人值守测试。
- [ ] **断线重连**：在请求中断线或复位后，验证主机重新握手与全量同步，不复用旧缓存。
- [ ] **安全关机**：按 5.6 完成停机，确认 `SLEEP`、`OFF`、`IDLE` 和所有危险输出关闭后再断开。

### 8.2 快速排查

| 现象 | 优先检查 | 处理原则 |
| --- | --- | --- |
| 版本响应不匹配或无响应 | 串口参数、完整行终止、连接状态 | 不开启危险控制；重新打开串口并握手。 |
| `SENSE_NOT_READY` | 是否刚上电、首个传感快照是否尚未发布 | 只延迟重试 `SENSE`，不否定已完成的版本握手。 |
| `OK` 后状态未变化 | 命令类别、保护、当前模式和回读时机 | 查询 `OUTPUT`/`MOTOR`，不要仅按 `OK` 更新最终 UI。 |
| 充电请求后未见 ON | `POWER` 与 `CHARGE_PHASE`、充电周期 | `CHARGE_PHASE=OFF` 是周期内的合法相位；在合适时机复查。 |
| 驱动子项不能开启 | 是否已确认 `POWER=DRIVE`、保护状态 | 先进入 DRIVE 并回读；不绕过联锁。 |
| 电机未按目标运行 | `MOTOR`、`FAULT`、堵转/温度状态 | 先停止并查询，再排除负载/保护原因。 |
| 下游消息混入响应 | 行分类与事件分流 | 把 UART2/3 事件从当前请求状态机中剥离。 |

## 9. 兼容性与版本维护

- 本文仅适用于 `release-v3.3`。主机应把版本字符串视为控制许可条件，而不是展示信息。
- 新固件如改变命令、状态字段、默认值、保护阈值、恢复语义或下游事件格式，应同步更新本主文档和[上位机 AT 命令文档](./上位机AT命令文档.md)，并为主机提供明确的版本适配策略。
- 业务层配置不应假设跨复位保存；本版本仅将 `STALL_CURRENT` 持久化。`CHARGE_TIME`、连接状态、桥接设置和输出请求应在每次连接后重新读取或重新配置。
- 当文档与现场观测冲突时，先停止危险控制，保存原始收发记录和版本信息，并以经验证的固件发布说明、协议参考及硬件规格为准。

## 10. 附录：固件运行机制简述

模块把外部可见行为分为以下协作职责：

| 职责 | 外部可见结果 |
| --- | --- |
| AT 接收解析 | 将 UART1 的完整命令行转为同步响应或控制请求；控制请求可能排队。 |
| 下游桥接 | 将下游数据回传为 UART2/3 异步事件，并可能与同步响应穿插。 |
| 传感采样 | 发布约 **1 Hz** 更新的传感快照；刚启动时快照可能尚未就绪。 |
| 电机控制 | 按请求完成唤醒、运行、换向、停止与安全停机，并提供状态回读。 |
| LED 控制 | 根据允许的主控请求与电机方向呈现联动状态。 |
| 输出/电源控制 | 处理 OFF、CHARGE、DRIVE 的互锁、充电相位及辅助输出关闭逻辑。 |
| 启动指示 | 上电阶段提供独立启动指示，不替代版本握手和状态同步。 |

因此，集成方应把“请求已接受”“当前状态已回读”和“下游事件已处理”视为不同阶段：请求可能排队，传感快照约 1 Hz 更新，异步事件也可能穿插在任意同步请求的响应流中。
