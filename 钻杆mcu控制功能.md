一个基于stm32f103c8t6开发的小项目，使用FreeRTOS作为实时操作系统，项目主要实现mcu对芯片多路adc电压的采集而后转换成物理量，对电机驱动芯片DRV8874的驱动，与上位机的通讯等，主要入口通过uart1实现，并在需要时将上位机的数据流通过uart2或者uart3转发；需要的功能有；
1.一个独立的任务，用于从uart1实时接受（非阻塞）at指令，设计一个at指令解析模块，把指令要求移交给对应的任务或者模块；uart123对应的引脚见cubemx生成的工程文件；
比如假设打开uart2的传输的at指令是AT+UART2=ON,打开之后我的其他任务应该也要正常运行，从uart1进来的其他at指令能被正常解析，没在指令集以外的内容从uart2发出去；大概需要分别实现AT+UART2=ON/OFF|AT+UART3=ON/OFF|AT+UART2&3=ON/OFF，只开关uart2，只开关uart3，同时开关uart23；
2.一个独立的任务，实现adc的实时采集（按照1hz的频率吧），并将采集到的电压转化为对应的物理量之后存到一个结构体变量中（这个变量是实时刷新的）（用一个信号量显示这个结构体能否被读取的状态，等上位机的读取指令，接受到读取指令后先判断能否被读取，如果能读取则读取最新的一次）；涉及两路adc（adc1和adc2）一共有6个通道，但是实际上有5个物理量是在这个任务中被使用了，adc对应的物理量大致如下：
```
ADC1_IN4-->电池NTC
ADC1_IN5-->电池电压

ADC2_IN6-->NTC3位置的电压（后面转化为温度）
ADC2_IN7-->NTC2位置的电压
ADC2_IN9-->NTC1位置的电压
需要注意的是上述三个电压使用的ntc都是一样的（但是电池的NTC我不知道），关于如何转化成温度可以留一个空函数后面迭代；上面的物理量也一样；
ADC2_IN8-->电机驱动芯片DRV8874PWPR的IPROPI引脚（挂下拉电阻），检测分压电阻的电压；需要注意的是ADC2_IN8没有在这个任务中被使用（主要是监控电机电流的），所以涉及资源竞态的问题，需要使用互斥锁保证高优先级任务使用的时候不被打扰；
```

3.一个独立的任务实现电机的控制，电机的控制芯片使用DRV8874，其中mcu连接了五个引脚如下：
```
EN/IN1-->使能引脚；
PH/IN2-->高前进，低后退；
nSLEEP-->低为睡眠，高的时候才能进行其他的控制；
nFault-->是一个错误反馈，具体的我也不太清楚，但是应该可以作为一个GPIO input或者外部中断去处理（就是读引脚反正只有0和1两个状态或者被通知状态，可以先不写错误处理流程后面再说）；
ADC2_IN8--IPROPI，就是一个电流读取的adc，要注意处理这个adc2的资源问题
```
大致的逻辑就是通过上位机接受at指令，完成开关机，驱动电机正反转（前进后退），然后通过实时读取adc的值（代表电机电流判断是否卡死）（注意adc资源的互斥），反正正常工作应该是再3A以下，先以这个为阈值设计函数；

具体的使用参考这个(./motor_driver_ph_en_mode_prompt.md)prompt（当前目录下），仅供参考；

4.两个led的控制，对应的引脚名称为LED1和LED3（电路改版后取消了原来的LED2/PA8，PA8改用作MP4317的NMOS控制），其中LED1作为mcu的上电指示灯（启动时与LED3交替闪烁5秒自检，正常运行后随电机状态变化），LED1和LED3分别作为电机前进和后退的指示灯（拉高拉低PH/IN2控制电机对应的两种工况），用一个单独的任务去控制，同时预留两个at指令，AT+LED=ON/OFF,作为总的led开关；对应的引脚为（已设好用户标签）：
```
PC15--LED1
PA15--LED3
```
5.其他控制：nmos控制，也是通过uart1进的at指令，单独一个任务实现对nmos1和nmos2的控制，对应的GPIO引脚如下，默认为低电压（关断）
```
PA12--NMOS1
PB4--NMOS2
```
LM51770 使能控制：通过 PB3（EN/UVLO）控制 LM51770 芯片的使能。LM51770 是低电平使能，所以默认拉高（LM51770 关），拉低时 LM51770 工作。v3.0 起不再提供独立芯片指令，使用 `AT+CHARGE=ON/OFF`：
```
PB3--EN/UVLO（LM51770 使能）默认高电平（关断）
```
MP4317 控制（电路改版新增）：原 LED2/PA8 改为推挽输出，驱动一个 NMOS 来控制 MP4317 的开关。MP4317 是低电平使能，所以默认拉高（MP4317 关），拉低时 MP4317 开。v3.0 起使用 `AT+DRIVE=ON/OFF`，MCU 在打开任一路前会先关闭两路：
```
PA8--MP4317 默认高电平（关断）
```
nFLT也是另外一个芯片的错误控制，理论上是应该作为一个中断处理，但是我现在是用的input模式，能主动读取就行；和nFault一样处理；
```
PA11--nFLT
```

## v2.2 实现同步说明（2026-07-26）

本节在原始需求说明后增量记录当前实现，不改写前述需求背景。

1. 固件版本已更新为 `release-v2.2`，上位机仍通过 `AT+VERSION?` 完成握手。
2. UART2/UART3 桥接开关命令保持兼容：
   - `AT+UART2=ON/OFF`
   - `AT+UART3=ON/OFF`
   - `AT+UART2&3=ON/OFF`
3. 新增二进制安全发送命令 `AT+UARTTX=<HEX>`：
   - 单帧负载为 1～32 字节；
   - `<HEX>` 必须为大写、偶数长度且不含分隔符；
   - 两路同时启用时向 UART2、UART3 发送相同原始字节；
   - 两路关闭、Hex 非法、底层发送失败时分别返回 `ERROR:UART_DISABLED`、`ERROR:HEX`、`ERROR:UART_TX`。
4. UART2/UART3 已启用逐字节中断接收和独立环形缓冲。ISR 只负责入队和重新挂接接收，不在中断内格式化或发送数据。
5. 新增 `bridgeTask`，在任务上下文把下游接收数据按最多 32 字节编码为：

   ```text
   +UART2RX:<HEX>\r\n
   +UART3RX:<HEX>\r\n
   ```

   这两类消息是异步事件，不附带 `OK`，上位机需要按来源独立处理。
6. UART1 的任务态发送增加互斥保护，避免普通 AT 回包与异步 UART 事件在字节层交错。
7. `AT+DIAG?` 新增 UART2/UART3 接收字节和环形缓冲溢出计数；已废弃的 `UART_WDG` 不再属于当前协议。
8. CMake 构建完成后会同步生成 `stem-hub.elf`、`stem-hub.hex` 和 `stem-hub.bin`，避免烧录文件停留在旧版本。

完整的帧格式、错误码和联调顺序见 [上位机AT命令文档.md](上位机AT命令文档.md)。

## v3.0 电源互锁与采样平均说明（2026-07-30）

1. LM51770 与 MP4317 只允许全关、仅 LM51770 开（充电）、仅 MP4317 开（驱动）三种状态。
2. AT 指令为 `AT+CHARGE=ON/OFF`、`AT+DRIVE=ON/OFF` 和 `AT+POWER=OFF`；旧的 `AT+LM51770`、`AT+MP4317` 指令已取消。
3. `AT+CHARGE=ON` 与 `AT+DRIVE=ON` 都由 MCU 输出任务先关闭两路，再打开目标芯片，不能由上位机组合出双开状态。
4. 只有五路 ADC 在同一周期全部成功时才同步推进窗口；BATT_NTC、BATT_V、NTC1、NTC2、NTC3 使用最近五个完整成功采样周期的原始 ADC 滑动均值。窗口、当前周期暂存和均值结果均位于静态 RAM，不占用 sensorTask 的局部栈；电机电流因参与过流保护不做该平均。

## v3.1 LM51770 间歇充电说明（2026-07-30）

1. 一次 `AT+CHARGE=ON` 启动持续的 10 秒充电、50 秒全关循环；循环持续到 OFF、DRIVE 或 MCU 复位。
2. 重复发送 `AT+CHARGE=ON` 不重置当前阶段计时。`AT+CHARGE=OFF`、`AT+DRIVE=OFF`、`AT+POWER=OFF` 和 `AT+DRIVE=ON` 都能立即中断循环。
3. 每次重新打开 LM51770 仍执行“先关 LM51770、再关 MP4317、只开 LM51770”的互锁顺序。计时使用绝对 RTOS Tick 截止值，不新增任务或任务栈，并支持 32 位 Tick 回绕。
4. 上位机 CHARGE 开关表示循环已启用，不代表 LM51770 EN 此刻一定处于开启阶段；本版不新增相位查询指令。
5. 该循环只是临时降额措施，不使用 NTC，也没有电流、累计时长或自动故障锁存保护。10/50 秒不构成安全保证，查明实际电流、功率器件和散热问题前不得进行无人值守满功率测试。
