# stem-hub 模块操作说明

> 适用固件：`release-v3.3`
> 主要读者：设备操作、现场测试和维护人员
> 集成说明：[stem-hub 模块集成与使用说明](./stem-hub模块集成与使用说明.md)
> 协议参考：[上位机 AT 命令文档](./上位机AT命令文档.md)

本手册假设模块已由集成工程师正确安装，且上位机已提供对应的操作入口。操作人员默认通过上位机界面执行，不假设任何具体按钮名称。

供电、接线、物理接口和安装要求以硬件规格书为准；本手册不替代硬件规格，也不能作为硬件额定值或负载能力的依据。

## 1. 操作前须知

1. 确认作业区域、机构、负载和人员均处于可安全启动状态。
2. 确认上位机已连接到本模块，并能显示版本、输出、电机和故障状态。
3. 仅当上位机按[集成说明](./stem-hub模块集成与使用说明.md)完成规定的温度门控，并明确显示或确认“允许操作”时，才可开启充电、驱动、电机或辅助输出。操作人员不得以一次 `SENSE` 自行判定允许开启。
4. 每次控制请求后，等待界面给出完整结果；一次只执行一个控制请求。
5. 界面显示未知、缺失、未明确允许操作或长时间未刷新时，保持危险输出关闭，先按第 7 章处置；无法确认门控时交由集成工程师。

**确认结果：** 操作人员能够看到可信的当前状态，且没有未确认的控制请求或现场风险。

## 2. 连接与启动确认

1. 接通系统规定的电源并打开上位机连接，等待连接状态稳定。
2. 在界面确认版本为 `release-v3.3`。版本不匹配时，不进入充电、驱动、电机或辅助输出操作。
3. 刷新并查看输出、电机、故障和传感状态。刚上电时传感状态可能稍后才出现。
4. 确认初态安全：电源为 `OFF`、充电相位为 `IDLE`、电机为 `SLEEP`，辅助输出和两类旁路均为关闭。

**确认结果：** 版本为 `release-v3.3`，状态能够刷新，且设备处于安全初态。

界面无法判断时，可由维护人员使用最小诊断查询：`AT+VERSION?`、`AT+OUTPUT?`、`AT+MOTOR?`、`AT+FAULT?` 和 `AT+SENSE?`。精确报文和返回字段见[上位机 AT 命令文档](./上位机AT命令文档.md)。

## 3. 充电操作

1. 完成第 2 章启动确认，并确认硬件规格允许当前电池、负载和环境进入充电；仅当上位机已完成规定的温度门控并明确显示或确认允许操作时，才继续。界面无法确认时，保持关闭并交由集成工程师。
2. 在界面请求进入充电模式。
3. 刷新输出状态，确认 `POWER=CHARGE`；继续观察 `CHARGE_PHASE`。其为 `ON` 或 `OFF` 都可能是充电周期中的正常状态。
4. 如需使用充电旁路，先由集成工程师或硬件负责人确认预充条件，再在确认 `POWER=CHARGE` 后请求开启。
5. 刷新输出状态，确认 `CHARGE_BYPASS` 的回读与请求一致；条件不满足或回读未开启时，保持旁路关闭。
6. 结束充电时，先关闭充电旁路，再请求关闭电源路径，并刷新状态。

**确认结果：** 进入后 `POWER=CHARGE`，并能看到 `CHARGE_PHASE` 的实际相位；退出后为 `POWER=OFF`、`CHARGE_PHASE=IDLE`，且 `CHARGE_BYPASS` 关闭。

不要只凭一次 `OK` 或瞬时输出判断已开始充电；`OK` 仅表示请求被接受，`CHARGE_PHASE` 才表示当时的实际充电相位。

## 4. 驱动操作

1. 完成第 2 章启动确认，并确认当前负载和现场条件满足硬件规格；仅当上位机已完成规定的温度门控并明确显示或确认允许操作时，才继续。界面无法确认时，保持关闭并交由集成工程师。
2. 在界面请求进入驱动模式。
3. 刷新输出状态，确认 `POWER=DRIVE`。
4. 仅在确认 `POWER=DRIVE` 后，逐项开启所需辅助输出；每开一项都刷新状态确认。
5. 结束驱动时，先关闭辅助输出，再请求关闭电源路径，并刷新状态。

**确认结果：** 使用期间 `POWER=DRIVE`，各辅助输出的回读与操作一致；退出后为 `POWER=OFF`、`CHARGE_PHASE=IDLE`，辅助输出均关闭。

## 5. 电机操作

1. 完成第 2 章启动确认，确认机构无人员接触风险、负载可自由运动；仅当上位机已完成规定的温度门控并明确显示或确认允许操作时，才继续。不能由一次 `SENSE` 自行判定保护门控已满足；界面无法确认时保持关闭并交由集成工程师。
2. 刷新电机和故障状态；存在堵转、故障输入有效、过温提示或状态不可信时，不启动电机。
3. 在界面请求所需方向或运行方式，并刷新电机状态确认目标模式。
4. 如需电机旁路，只在确认电机稳定处于正向或反向运行后开启，并通过输出状态确认；换向前或停止前先关闭旁路。
5. 普通停止时，请求 `STOP` 或 `BRAKE` 并确认相应停止模式。两者是不同的界面状态，均不等同于休眠。
6. 需要最低静态功耗、准备断开或完成安全关机时，再请求 `SLEEP` 并刷新确认。

**确认结果：** 运行后的模式与所选目标一致；普通停止后为 `STOP` 或 `BRAKE`，安全关机后为 `SLEEP`；停止后电机旁路关闭。

## 6. 状态与故障提示

本章只说明状态含义；“立即做什么”和“恢复条件”以第 7 章为唯一完整处置流程，避免在两处执行不同动作。

| 看到什么 | 代表什么 | 转入的完整处置 |
| --- | --- | --- |
| 版本不是 `release-v3.3` | 当前上位机与模块版本不匹配 | 第 7 章“版本不匹配” |
| 状态不刷新、长时间无响应或通信中断 | 当前实际状态未知；此前请求可能已执行，也可能未执行 | 第 7 章“状态不刷新或通信中断” |
| `SENSE_NOT_READY` | 刚启动时尚未取得首个传感快照 | 第 7 章“`SENSE_NOT_READY`” |
| 过温保护或开启被温度限制 | 受保护温度异常、无效或超过允许范围，设备已进入或应保持安全状态 | 第 7 章“过温保护” |
| 电机堵转或过流提示 | 机构可能卡滞、负载异常或电机保护已动作 | 第 7 章“电机堵转” |
| `nFAULT` 有效 | 驱动相关故障输入处于有效状态 | 第 7 章“`nFAULT` 有效” |
| `nFLT` 有效 | 辅助故障输入处于有效状态 | 第 7 章“`nFLT` 有效” |
| 命令显示成功但状态未改变 | 请求已被接受，但实际状态尚未达到目标、受联锁限制，或回读时机不足 | 第 7 章“命令成功但状态未变” |

`SENSE` 是必要的观察信息，但不是保护解除或危险开启的充分依据；它可能是较早的快照。当前没有可供操作人员使用的温度锁存状态查询。

`nFAULT`/`nFLT` 仅用于查询故障输入；固件不会仅因它们自动停机，也不承诺自动恢复。电机状态中的 `MOTOR.FAULT` 可能是旧快照；判断实时故障时，以故障状态查询为准。

## 7. 异常处置

遇到任一异常，先让设备回到可确认的安全状态，再恢复操作。本章是第 6 章所列故障的唯一完整处置流程。

### 完整安全停机顺序

1. 关闭电机旁路。
2. 请求 `STOP` 或 `BRAKE`。
3. 请求 `SLEEP`。
4. 关闭其他旁路和全部辅助输出。
5. 请求关闭电源路径。
6. 刷新状态，确认电机为 `SLEEP`，电源为 `OFF`，充电相位为 `IDLE`，旁路和辅助输出均关闭。

| 看到什么 | 代表什么 | 立即做什么 | 恢复条件 |
| --- | --- | --- | --- |
| 版本不匹配 | 控制语义不能确认 | 保持危险输出关闭，记录版本信息 | 集成工程师完成匹配或适配，并重新启动确认 |
| 状态不刷新或通信中断 | 无法确认设备是否已按请求动作 | 停止连续重试；通信仍可用时执行上方完整安全停机顺序；不能通信时按系统安全策略隔离风险 | 集成工程师重连后，重新确认版本并刷新输出、电机、故障和传感状态 |
| `SENSE_NOT_READY` | 尚无首个传感快照 | 保持危险输出关闭，等待后重试传感状态 | 上位机按主文档完成规定温度门控并明确显示或确认允许操作；操作人员再人工发起新请求并用目标状态回读确认 |
| 过温保护 | 温度保护正在或曾经介入 | 不恢复旧输出，检查通风、负载和环境；通信可用时依次关闭电机旁路、直接请求 `SLEEP`（不请求 `STOP`/`BRAKE`）、关闭其他旁路和全部辅助输出、关闭电源路径，最后回读确认。任一关闭动作报错时，仍继续尝试其余安全关闭，并升级维护 | 上位机先按[集成说明](./stem-hub模块集成与使用说明.md)完成连续新鲜快照等必要预检查，并明确允许操作人员人工尝试；操作人员再人工发起新的危险开启请求。该请求由固件在执行时作最终保护裁决；仅当未被 `ERROR:OVER_TEMPERATURE` 拒绝且目标状态回读符合预期，才算恢复。若被拒绝或回读不符，继续保持关闭；绝不自动恢复 |
| 电机堵转 | 机构可能卡滞、负载异常或电机保护已动作 | 执行上方完整安全停机顺序；先排除机械和负载原因 | 排除原因后，刷新电机和故障状态；上位机明确允许操作后，再人工重新启动并回读确认 |
| `nFAULT` 有效 | 驱动相关故障输入处于有效状态 | 关闭电机旁路，依次请求 `STOP` 或 `BRAKE`、`SLEEP`，再关闭可能相关的驱动和辅助输出；无法判断关联对象时执行上方完整安全停机顺序，并交由维护或集成工程师 | 故障源排除、故障状态恢复正常且上位机明确允许操作后，再人工重新请求并回读确认 |
| `nFLT` 有效 | 辅助故障输入处于有效状态 | 停止相关危险功能；无法判断关联对象时执行上方完整安全停机顺序，并交由维护或集成工程师 | 故障源排除、故障状态恢复正常且上位机明确允许操作后，再人工重新请求并回读确认 |
| 命令成功但状态未变 | 请求与实态不一致，不能假定操作完成 | 保持或恢复关闭状态，刷新输出、电机、故障和传感状态 | 目标状态已回读确认；若仍不一致，交由集成工程师诊断 |

## 8. 停机与断开

1. 关闭电机旁路。
2. 请求 `STOP` 或 `BRAKE`。
3. 请求 `SLEEP`。
4. 关闭其他旁路和全部辅助输出。
5. 请求关闭电源路径。
6. 刷新电机和输出状态，确认电机为 `SLEEP`，电源为 `OFF`，充电相位为 `IDLE`，辅助输出和两类旁路均关闭。
7. 确认无待处理的故障或控制请求后，再断开上位机连接和系统规定的外部连接。

**确认结果：** 电机为 `SLEEP`；输出显示 `POWER=OFF`、`CHARGE_PHASE=IDLE`；辅助输出、`MOTOR_BYPASS` 和 `CHARGE_BYPASS` 均为关闭，然后才允许断开。

## 9. 禁止事项

- 禁止跳过版本检查后直接进行充电、驱动、电机或辅助输出操作。
- 禁止并发或连续发送多个控制请求；必须等待当前结果和必要的状态回读。
- 禁止只凭 `OK` 更新最终状态或判断操作成功。
- 禁止在保护解除后自动恢复旧的充电、驱动、电机或其他危险输出。
- 禁止无人值守带载运行、充电、驱动或电机测试。
- 禁止把本软件说明当作供电、负载、接口、热设计或其他硬件额定值依据。
- 禁止未经硬件负责人批准进行满功率硬堵转，或物理加热带载器件。
