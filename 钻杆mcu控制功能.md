一个基于stm32f103c8t6开发的小项目，使用FreeRTOS作为实时操作系统，项目主要实现mcu对芯片多路adc电压的采集而后转换成物理量，对电机驱动芯片DRV8874的驱动，与上位机的通讯等，主要入口通过uart1实现，并在需要时将上位机的数据流通过uart2或者uart3转发；需要的功能有；
1.一个独立的任务，用于从uart1实时接受（非阻塞）at指令，设计一个at指令解析模块，把指令要求移交给对应的任务或者模块；uart123对应的引脚见cubemx生成的工程文件；
比如假设打开uart2的传输的at指令是AT+UART2=ON,打开之后我的其他任务应该也要正常运行，从uart1进来的其他at指令能被正常解析，没在指令集以外的内容从uart2发出去；大概需要分别实现AT+UART2=ON/OFF|AT+UART3=ON/OFF|AT+UART2&3=ON/OFF，只开关uart2，只开关uart3，同时开关uart23；
2.一个独立的任务，实现 ADC 的实时采集（1 Hz），并将采集到的电压转换成物理量后发布为同步快照；BATT_NTC、BATT_V 与五路器件 NTC 使用最近五个完整采样周期的同步滚动窗口。物理映射如下：
```
PA4（物理引脚14）/ ADC1_IN4 --> BATT_NTC，电池 NTC
PA5 / ADC1_IN5 --> BATT_V，电池电压
PB1（物理引脚19）/ ADC2_IN9 --> MCU_C
PA7（物理引脚17）/ ADC2_IN7 --> LM51770_C
PA6（物理引脚16）/ ADC2_IN6 --> MP4317_C
PA1（物理引脚11）/ ADC2_IN1 --> DRV8874_C
PA0（物理引脚10）/ ADC1_IN0 --> CHARGE_MOS_C，LM51770 充电 MOS
PB0 / ADC2_IN8 --> MOTOR_I，DRV8874PWPR 的 IPROPI 电流检测
```
五路器件温度均使用 HNTC0603-103F3450FA 和 `3V3 -- NTC -- ADC -- 470 Ω -- GND` 拓扑，共用 -40~+125°C 查找表。BATT_NTC 保留独立的 3435K 查找表并且仅显示。ADC2_IN8 不进入七通道窗口，继续通过互斥锁与传感任务协调并保留即时过流语义。

3.一个独立的任务实现电机的控制，电机的控制芯片使用DRV8874，其中mcu连接了五个引脚如下：
```
EN/IN1-->使能引脚；
PH/IN2-->高前进，低后退；
nSLEEP-->低为睡眠，高的时候才能进行其他的控制；
nFault-->是一个错误反馈，具体的我也不太清楚，但是应该可以作为一个GPIO input或者外部中断去处理（就是读引脚反正只有0和1两个状态或者被通知状态，可以先不写错误处理流程后面再说）；
ADC2_IN8--IPROPI，就是一个电流读取的adc，要注意处理这个adc2的资源问题
```
大致的逻辑就是通过上位机接受 AT 指令，完成开关机，驱动电机正反转（前进后退），并实时读取 ADC 电流判断是否卡死（注意 ADC 资源互斥）。当前实现按实测额定负载约 3.2 A 选用 4.0 A 默认堵转阈值，并使用启动屏蔽加连续超限判定。

具体的使用参考这个(./motor_driver_ph_en_mode_prompt.md)prompt（当前目录下），仅供参考；

4.两个led的控制，对应的引脚名称为LED1和LED3（电路改版后取消了原来的LED2/PA8，PA8改用作MP4317的NMOS控制），其中LED1作为mcu的上电指示灯（启动时与LED3交替闪烁5秒自检，正常运行后随电机状态变化），LED1和LED3分别作为电机前进和后退的指示灯（拉高拉低PH/IN2控制电机对应的两种工况），用一个单独的任务去控制，同时预留两个at指令，AT+LED=ON/OFF,作为总的led开关；对应的引脚为（已设好用户标签）：
```
PC15--LED1
PA15--LED3
```
5.其他控制：nmos控制，也是通过uart1进的at指令，单独一个任务实现对nmos1和nmos2的控制，对应的GPIO引脚如下，默认为低电压（关断）
```
PA12--NMOS1
PB4--NMOS2
```
LM51770 使能控制：通过 PB3（EN/UVLO）控制 LM51770 芯片的使能。LM51770 是低电平使能，所以默认拉高（LM51770 关），拉低时 LM51770 工作。v3.0 起不再提供独立芯片指令，使用 `AT+CHARGE=ON/OFF`：
```
PB3--EN/UVLO（LM51770 使能）默认高电平（关断）
```
MP4317 控制（电路改版新增）：原 LED2/PA8 改为推挽输出，驱动一个 NMOS 来控制 MP4317 的开关。MP4317 是低电平使能，所以默认拉高（MP4317 关），拉低时 MP4317 开。v3.0 起使用 `AT+DRIVE=ON/OFF`，MCU 在打开任一路前会先关闭两路：
```
PA8--MP4317 默认高电平（关断）
```
nFLT也是另外一个芯片的错误控制，理论上是应该作为一个中断处理，但是我现在是用的input模式，能主动读取就行；和nFault一样处理；
```
PA11--nFLT
```

## v2.2 实现同步说明（2026-07-26）

本节在原始需求说明后增量记录当前实现，不改写前述需求背景。

1. 固件版本已更新为 `release-v2.2`，上位机仍通过 `AT+VERSION?` 完成握手。
2. UART2/UART3 桥接开关命令保持兼容：
   - `AT+UART2=ON/OFF`
   - `AT+UART3=ON/OFF`
   - `AT+UART2&3=ON/OFF`
3. 新增二进制安全发送命令 `AT+UARTTX=<HEX>`：
   - 单帧负载为 1～32 字节；
   - `<HEX>` 必须为大写、偶数长度且不含分隔符；
   - 两路同时启用时向 UART2、UART3 发送相同原始字节；
   - 两路关闭、Hex 非法、底层发送失败时分别返回 `ERROR:UART_DISABLED`、`ERROR:HEX`、`ERROR:UART_TX`。
4. UART2/UART3 已启用逐字节中断接收和独立环形缓冲。ISR 只负责入队和重新挂接接收，不在中断内格式化或发送数据。
5. 新增 `bridgeTask`，在任务上下文把下游接收数据按最多 32 字节编码为：

   ```text
   +UART2RX:<HEX>\r\n
   +UART3RX:<HEX>\r\n
   ```

   这两类消息是异步事件，不附带 `OK`，上位机需要按来源独立处理。
6. UART1 的任务态发送增加互斥保护，避免普通 AT 回包与异步 UART 事件在字节层交错。
7. `AT+DIAG?` 新增 UART2/UART3 接收字节和环形缓冲溢出计数；已废弃的 `UART_WDG` 不再属于当前协议。
8. CMake 构建完成后会同步生成 `stem-hub.elf`、`stem-hub.hex` 和 `stem-hub.bin`，避免烧录文件停留在旧版本。

完整的帧格式、错误码和联调顺序见 [上位机AT命令文档.md](上位机AT命令文档.md)。

## v3.0 电源互锁与采样平均说明（2026-07-30）

1. LM51770 与 MP4317 只允许全关、仅 LM51770 开（充电）、仅 MP4317 开（驱动）三种状态。
2. AT 指令为 `AT+CHARGE=ON/OFF`、`AT+DRIVE=ON/OFF` 和 `AT+POWER=OFF`；旧的 `AT+LM51770`、`AT+MP4317` 指令已取消。
3. `AT+CHARGE=ON` 与 `AT+DRIVE=ON` 都由 MCU 输出任务先关闭两路，再打开目标芯片，不能由上位机组合出双开状态。
4. BATT_NTC、BATT_V、MCU_C、LM51770_C、MP4317_C、DRV8874_C、CHARGE_MOS_C 七路只有在同一周期全部成功时才同步推进并发布；任何部分周期都不推进、不发布。各路使用最近五个完整采样周期的原始 ADC 滑动均值。窗口、当前周期暂存和均值结果均位于静态 RAM，不占用 sensorTask 的局部栈；电机电流因参与过流保护不做该平均。

## v3.1 LM51770 间歇充电说明（2026-07-30）

1. 一次 `AT+CHARGE=ON` 启动持续的 10 秒充电、50 秒全关循环；循环持续到 OFF、DRIVE 或 MCU 复位。
2. 重复发送 `AT+CHARGE=ON` 不重置当前阶段计时。`AT+CHARGE=OFF`、`AT+DRIVE=OFF`、`AT+POWER=OFF` 和 `AT+DRIVE=ON` 都能立即中断循环。
3. 每次重新打开 LM51770 仍执行“先关 LM51770、再关 MP4317、只开 LM51770”的互锁顺序。计时使用绝对 RTOS Tick 截止值，不新增任务或任务栈，并支持 32 位 Tick 回绕。
4. 上位机 CHARGE 开关表示循环已启用，不代表 LM51770 EN 此刻一定处于开启阶段；v3.3 可通过 `AT+OUTPUT?` 的 `CHARGE_PHASE` 查询实际相位。
5. 该循环的时间配置不构成独立安全保证；当前 v3.2 另有五路器件 NTC 软件保护，但仍不能替代硬件限流、功率器件选型和散热设计。查明实际电流、功率器件和散热问题前不得进行无人值守满功率测试。

## v3.2 语义化温度遥测与 NTC 停机

固件与上位机通过 `+VERSION:release-v3.3` + `OK` 精确握手。`AT+SENSE?` 按固定顺序返回语义化传感字段；`AT+OUTPUT?` 固定返回请求电源模式、实际充电相位和全部输出应用状态。

1. `AT+CHARGE_TIME=n` 将固定 60 秒周期中的 ON 时间设置为 1～60 秒；`AT+CHARGE_TIME=?` 精确返回 `+CHARGE_TIME:<n>\r\nOK\r\n`。默认值为 10，只保存于 RAM，复位恢复 10 秒 ON / 50 秒 OFF。
2. 运行中修改配置不改变当前 ON/OFF 相位及其绝对截止 Tick，从下一次 ON 相位开始对整个新周期生效。`n=60` 为连续 ON，内部 60 秒边界不切 EN，不让 LM51770 每分钟重新软启动。CHARGE 开关仍只表示循环启用，不表示 EN 此刻必然打开。
3. MCU_C、LM51770_C、MP4317_C、DRV8874_C、CHARGE_MOS_C 在七路采样全部成功时使用与 `AT+SENSE?` 相同的最近五个完整周期均值。若 BATT_NTC 或 BATT_V 失败而五路器件样本成功，保护使用正式同步窗口加本周期器件样本计算只读预览均值，不推进窗口、不发布 SENSE。任一路严格高于 60.0°C、温度换算无效或 ADC 读取失败，即锁存过温保护；BATT_NTC 仅显示。
4. 触发后取消 CHARGE/DRIVE、关闭 LM51770/MP4317、NMOS1/2、LIGHTS 和两路旁路，并让电机进入 SLEEP。停机使用高优先级队列，输出/电机消费者另以 100 ms 周期复查锁存，阻止旧的排队开启请求复活输出。
5. 只有五路受保护器件温度都有效且全部不高于 55.0°C 才解除锁存；解除只恢复人工命令权限，不自动恢复停机前状态。保护期间 OFF、MOTOR SLEEP、查询及 CHARGE_TIME 命令仍可用，危险开启命令返回 `ERROR:OVER_TEMPERATURE`；共享状态暂时不可读时返回 `ERROR:STATE_BUSY` 并按安全失败处理。
6. 五周期平均会引入最多约数秒的热响应延迟。该功能与可调充电占空均为测试/降额措施，不替代硬件限流、器件选型、散热和独立温度保护；禁止无人值守带载。文档不代表已经完成加热 NTC 的实机端到端测试。

## 电机堵转保护增量说明（2026-08-16）

1. DRV8874 IPROPI 的 R19 实装为 220 Ω，AIPROPI 按 450 µA/A：`I_mA = V_mV × 1000 / 99`，3.3 V ADC 对应约 33.3 A 满量程。空载约 0.5 A、额定负载约 3.2 A、理论堵转约 19 A 仅用于确定保护窗口。
2. 电机任务每 10 ms 采样。FWD/REV 启动后屏蔽 300 ms；屏蔽结束后，电流连续不低于阈值 100 ms 才触发，随后 EN 拉低、nSLEEP 保持高、PH 保持原方向，状态转为 BRAKE 并锁存 `OVERCURRENT=1`。单个无效 ADC 样本或低于阈值的样本会清除本次持续计时。
3. 阈值默认 4000 mA，合法范围 1000～30000 mA。`AT+STALL_CURRENT=<mA>` 设置并写入 STM32 最后一页 1 KiB Flash，`AT+STALL_CURRENT=?` 查询；记录由 magic、格式版本和 CRC32 校验，损坏或未初始化时回退到 4000 mA。
4. FWD/REV 运行时禁止擦写阈值并返回 `ERROR:MOTOR_RUNNING`；Flash 擦写或写后校验失败返回 `ERROR:FLASH_WRITE`。相同值不重复擦写。
5. 堵转后正反两个方向都允许再次启动；新 FWD/REV 会清除旧锁存并重新开始 300 ms 屏蔽。若仍沿堵塞方向运行，随后会再次触发停机。SLEEP、WAKE、BRAKE、STOP 不清除旧锁存；过温保护仍具有更高优先级。
6. 该电流闭环是软件保护，不能替代 DRV8874 硬件保护、保险和机械限位。测试应降低阈值或使用可控负载，不故意制造 19 A 硬堵转。

## PC13/PC14 限流电阻旁路增量说明（2026-08-23）

1. UART1、UART2、UART3 均使用 9600 8N1；电机运行时普通 AT 解析及 UART2/UART3 通信继续工作，不使用独占透传模式。
2. PC13（STM32F103C8T6 物理 2 脚）配置为推挽输出且默认低电平，低电平保留电机启动限流电阻。电机进入 FWD/REV 后可发送 `AT+MOTOR_BYPASS=ON` 拉高并短接电阻；`AT+MOTOR_BYPASS=OFF` 恢复低电平。
3. PC13 的 ON 只在 FWD/REV 状态接受，否则返回 `ERROR:STATE`；状态暂不可读返回 `ERROR:STATE_BUSY`。STOP、BRAKE、SLEEP、堵转、过温及 FWD/REV 换向均自动拉低，换向后必须重新 ON。
4. PC14（物理 3 脚）配置为推挽输出且默认低电平。请求模式为 CHARGE 时即可用 `AT+CHARGE_BYPASS=ON` 拉高 PC14；它跨周期 OFF 相位锁存，使下一 ON 相位从开始即旁路限流电阻。
5. PC14 在退出 CHARGE、切换 DRIVE、过温、强制安全停机或复位时自动拉低。NMOS1、NMOS2、LIGHTS 只有 DRIVE 模式可开启，离开 DRIVE 自动关闭；所有 OFF 请求始终允许。
