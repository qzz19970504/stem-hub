一个基于stm32f103c8t6开发的小项目，使用FreeRTOS作为实时操作系统，项目主要实现mcu对芯片多路adc电压的采集而后转换成物理量，对电机驱动芯片DRV8874的驱动，与上位机的通讯等，主要入口通过uart1实现，并在需要时将上位机的数据流通过uart2或者uart3转发；需要的功能有；
1.一个独立的任务，用于从uart1实时接受（非阻塞）at指令，设计一个at指令解析模块，把指令要求移交给对应的任务或者模块；uart123对应的引脚见cubemx生成的工程文件；
比如假设打开uart2的传输的at指令是AT+UART2=ON,打开之后我的其他任务应该也要正常运行，从uart1进来的其他at指令能被正常解析，没在指令集以外的内容从uart2发出去；大概需要分别实现AT+UART2=ON/OFF|AT+UART3=ON/OFF|AT+UART2&3=ON/OFF，只开关uart2，只开关uart3，同时开关uart23；
2.一个独立的任务，实现adc的实时采集（按照1hz的频率吧），并将采集到的电压转化为对应的物理量之后存到一个结构体变量中（这个变量是实时刷新的）（用一个信号量显示这个结构体能否被读取的状态，等上位机的读取指令，接受到读取指令后先判断能否被读取，如果能读取则读取最新的一次）；涉及两路adc（adc1和adc2）一共有6个通道，但是实际上有5个物理量是在这个任务中被使用了，adc对应的物理量大致如下：
```
ADC1_IN4-->电池NTC
ADC1_IN5-->电池电压

ADC2_IN6-->NTC3位置的电压（后面转化为温度）
ADC2_IN7-->NTC2位置的电压
ADC2_IN9-->NTC1位置的电压
需要注意的是上述三个电压使用的ntc都是一样的（但是电池的NTC我不知道），关于如何转化成温度可以留一个空函数后面迭代；上面的物理量也一样；
ADC2_IN8-->电机驱动芯片DRV8874PWPR的IPROPI引脚（挂下拉电阻），检测分压电阻的电压；需要注意的是ADC2_IN8没有在这个任务中被使用（主要是监控电机电流的），所以涉及资源竞态的问题，需要使用互斥锁保证高优先级任务使用的时候不被打扰；
```

3.一个独立的任务实现电机的控制，电机的控制芯片使用DRV8874，其中mcu连接了五个引脚如下：
```
EN/IN1-->使能引脚；
PH/IN2-->高前进，低后退；
nSLEEP-->低为睡眠，高的时候才能进行其他的控制；
nFault-->是一个错误反馈，具体的我也不太清楚，但是应该可以作为一个GPIO input或者外部中断去处理（就是读引脚反正只有0和1两个状态或者被通知状态，可以先不写错误处理流程后面再说）；
ADC2_IN8--IPROPI，就是一个电流读取的adc，要注意处理这个adc2的资源问题
```
大致的逻辑就是通过上位机接受at指令，完成开关机，驱动电机正反转（前进后退），然后通过实时读取adc的值（代表电机电流判断是否卡死）（注意adc资源的互斥），反正正常工作应该是再3A以下，先以这个为阈值设计函数；

具体的使用参考这个(./motor_driver_ph_en_mode_prompt.md)prompt（当前目录下），仅供参考；

4.三个led的控制，对应的引脚名称为LED1LED2和LED3，其中LED1作为mcu的上电指示灯（应该是在主循环里面调用，led2和led3分别作为电机前进和后退，也就是拉高拉低PH/IN2控制电机对应的两种工况，用一个单独的任务去控制，同时预留两个at指令，AT+LED=ON/OFF,作为总的led开关；对应的引脚为（已设好用户标签）：
```
PC15--LED1
PA8--LED2
PA15--LED3
```
5.其他控制：nmos控制，也是通过uart1进的at指令，单独一个任务实现对nmos1和nmos2的控制，对应的GPIO引脚如下，默认为低电压（关断）
```
PA12--NMOS1
PB4--NMOS2
```
下面也是nmos，用于实现对芯片使能的控制，默认为低电压，拉高芯片断电，拉低芯片正常工作（默认为低）；
```
PB3--EN/UVLO 默认为低电平
```
nFLT也是另外一个芯片的错误控制，理论上是应该作为一个中断处理，但是我现在是用的input模式，能主动读取就行；和nFault一样处理；
```
PA11--nFLT
```